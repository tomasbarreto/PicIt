package com.example.picit.winner

import com.example.picit.entities.RePicPhoto

class PhotoComparator {

    private lateinit var modelPhoto: RePicPhoto

    fun setModelPhoto(modelPhoto: RePicPhoto) {
        this.modelPhoto = modelPhoto
    }
    fun comparePhoto(photoToCompare: RePicPhoto) {
        
    }
}